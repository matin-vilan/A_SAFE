// Attention: without matcher, this line protect all the website.
export { default } from "next-auth/middleware";

// only match routes below
// export const config = { matcher: ['/',"/dashboard"] };
